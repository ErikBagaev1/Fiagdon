import 'package:flutter/material.dart';

class CultureDisplayWidget extends StatelessWidget {
  const CultureDisplayWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
